name: ailogic_triggers
description: AI Logic Cloud Triggers written in Dart
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  firebase_functions: ^0.6.0

dev_dependencies:
  build_runner: ^2.15.1
  lints: ^6.0.0
